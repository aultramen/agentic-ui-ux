# Model routing: independent final-source decision exercise

These are exact source-based responses to the 12 supplied scenarios. **Swift, Deep, model capabilities, prices, and timing comparisons are synthetic fixtures.** This is not native host/UI execution: no product task, model/provider call, generation, switch, or review check was performed. Execution states below describe the scenario's next state, not completed work in this evaluation.

Read: the scenario JSON; current router and five phase skills; shared routing, artifacts, capabilities, discovery and quality references; style visual-exploration; routing, approvals, plan and verification templates. No rubric, previous evaluator responses/evidence, implementation plan, git diff, or other agents were consulted. `W` below means the existing matching task's `docs/ui-ux/work/<task-id>`; actual task IDs and unspecified production filenames must come from that task, not be invented. Scenario IDs identify these responses, not invented user approvals.

## MR-01

1. Stop before the first production edit. Read the settled task metadata and identify its production output batch; present the proposal.
2. **Card:** Phase: `ui-ux-build / implement`. Scope/artifacts: shared ticket review/submit, permissions and real API errors, existing planned production targets. Model: Deep. Effort: high. Reason: the permission/API risk warrants stronger reasoning; the fixture says approximately 4x cost and 2x latency versus Swift/medium. Cheaper alternative: Swift/medium, with less reasoning capacity and no established adequacy for this risk. Estimate: those synthetic relative figures only; total price and completion time unknown without workload assumptions.
3. **User message:** “Deep/high materially increases the expected cost and latency for this build. The routing contract requires your decision before production edits. Do you approve this configuration for the described batch?”
4. In `W/model-routing.md`, record scope/output identity, fixture provenance, recommendation, approval `pending`, execution `waiting-for-approval`, actual Swift/medium. Only after actual approval use the supported switch, confirm Deep/high and executor evidence, then begin edits.

Deadline, prior preparation and the senior's request do not supply the missing user decision. Independent authorized preparation may continue; verification remains unverified.

## MR-02

1. Read the existing routing decision and scope metadata; do not assess implementation or run checks while the material-cost decision is pending.
2. **Card:** Phase: `ui-ux-review / audit`. Scope/artifacts: accessibility and permission behavior across eight pages, findings/evidence in `W/verification/`. Model: Deep. Effort: high. Reason: breadth and permissions warrant stronger reasoning, with materially greater fixture cost. Cheaper alternative: Swift/medium, reduced review depth; adequacy for this scope is not established. Estimate: total cost/time unknown; no numerical multiplier was supplied.
3. **User message:** “The eight-page audit is waiting on the existing Deep/high cost decision. Read-only review has the same routing boundary before assessment or checks. Do you approve this configuration for the audit?” If the identical card/question is already visible, reference it instead of issuing another approval request.
4. Preserve the existing decision as `pending`, with execution `waiting-for-approval`; do not create a duplicate. Record actual settings only from available evidence. Upon approval, establish the selected executor/settings before starting the audit.

Design approval remains valid for its design scope. It supplies neither routing approval nor review evidence; audit criteria remain unverified.

## MR-03

1. Keep the completed brief, research and two prepared prompts. Pause immediately before generating any image or static visual artifact.
2. **Card:** Phase: `ui-ux-style / visual generation`. Scope/artifacts: two candidates in `W/styling/v001/`. Model: Swift; image generator: the available tool, provider/model tool-managed or unknown. Effort: reasoning medium; generator effort not exposed; candidate count two. Reason: reasoning is adequate unchanged, but generation adds a new material charge. Cheaper alternative: no priced adequate alternative is established; a disclosed static render would require its own method/impact assessment. Estimate: total cost/time unknown; two outputs assumed, material new charge supplied by the fixture.
3. **User message:** “Generating these two candidates adds a material charge. The routing contract requires your expenditure decision before generation. Do you approve using this image tool for the two v001 candidates?”
4. Record reasoning and generator identities separately in `W/model-routing.md`; decision `pending`, execution `waiting-for-approval`. After approval, establish actual reasoning/tool settings, then generate.

Prepared prompts and deadline do not authorize expenditure. Do not assign the image tool a fictional model or reasoning effort. Generation permission will still leave visual acceptance outstanding.

## MR-04

1. Retain the exact visual approval, then stop before writing `W/prototype/v001/index.html` or related prototype artifacts.
2. **Card:** Phase: `ui-ux-prototype / HTML generation`. Scope/artifacts: multi-page wizard with branching validation, retry/reset fixtures, `W/prototype/v001/index.html` and its required local assets. Model: Deep. Effort: high. Reason: branching behavior warrants stronger reasoning; fixture cost/time increase is material. Cheaper alternative: Swift/medium, lower reasoning capacity and unestablished adequacy for all branches. Estimate: totals unknown; material increase is known, no numeric rate or duration supplied.
3. **User message:** “The image approval covers visual direction. Deep/high adds material cost/time for the interactive wizard, so the routing contract requires a separate execution decision. Do you approve it for prototype/v001?”
4. Record approval `pending` and execution `waiting-for-approval`. After approval use the available automatic switch, verify actual Deep/high and executor evidence, then generate the isolated HTML/fixtures.

Disposable code is still gated. Keep the visual approval in `approvals.md`; link the routing decision separately. Later interaction approval must concern the actual complete scoped prototype, not the earlier image.

## MR-05

1. Select Swift/high for the small plan. Swift supports high and is the economical reasoning-capable option; medium is not the planning default even for a tiny correction.
2. **Card:** Phase: `ui-ux-plan / drafting`. Scope/artifacts: accepted-baseline alignment correction, short `W/plan.md`. Model: Swift. Effort: high. Reason: sufficient planning reasoning without a justified move to Deep. Cheaper alternative: Swift/medium, only on the user's explicit lower-effort choice. Estimate: amounts and duration unknown; fixture establishes only that medium costs less than high.
3. Assess actual material impact against the bounded scope and supplied evidence. A higher effort alone does not prove a material change. If no material impact is identified, record `not-required` with that rationale, then establish Swift/high before drafting. If evidence identifies a material impact, record `pending` and request approval with the concrete impact; drafting waits.
4. **User message:** “I recommend Swift/high for this short plan. Total cost and time are unknown. Swift/medium is cheaper but requires your explicit choice to lower planning effort.” Use an exposed switch if available; otherwise request manual Swift/high selection and confirmation and record `waiting-for-switch`.

The teammate's suggestion does not constitute the user's lower-effort choice. Record execution `ready` only after effective settings are established.

## MR-06

1. Preserve the prototype-only Deep/high approval. Discover the concrete planning scope and current supported choices before dispatching the plan; do not seek approval for unsized build/review phases.
2. **Next-phase card:** Phase: `ui-ux-plan / drafting`. Scope/artifacts: ticket-wizard production plan derived from the accepted HTML, `W/plan.md`. Model: Deep, retained if current support and adequacy are established. Effort: high. Reason: default planning effort and unchanged active configuration; reassess if sizing reveals substantial complexity. Cheaper alternative: none established by this fixture. Estimate: cost/time unknown; no rates or workload supplied.
3. **User message:** “I’m selecting settings for the production plan now. The prototype approval remains scoped to prototype/v001. Build and review will each receive a scoped routing decision when their work is sized.”
4. Create a distinct planning decision: `not-required` if there is no material impact/restriction, otherwise `pending` until the concrete proposal is approved. Confirm actual settings before drafting. The router's immediate unchanged card satisfies presentation; direct plan invocation validates and reuses that decision without asking twice.

Repeat sizing, selection, impact decision and effective-settings validation at build and review boundaries. Neither phase borrows prototype/planning approval. Keep design approvals and eventual delivery acceptance separate; authorized implementation needs no invented extra plan-approval ceremony.

## MR-07

1. Read MR-7 and its actual conversation source. Match `ui-ux-build / implement`, UX-01 checkbox alignment, Swift/high, the same production targets and output batch. The shorter wording and ordinary edit do not alter that semantic identity.
2. The router has already shown the resumed selection card. The direct build validates MR-7 and uses it; it neither repeats the card immediately nor requests another approval.
3. **User message:** “MR-7 still covers UX-01 with Swift/high and the same output batch. The effective settings are confirmed, so I’m resuming the build under that approval.”
4. Preserve the original `approved` decision and provenance in `W/model-routing.md`. Append an execution-history entry referencing MR-7, actual Swift/high and the supplied confirmation source, with state `ready` before edits. Record the changed source snapshot as execution/verification evidence, not a new approval identity.

Resume at the first unmet dependency. Perform affected verification after relevant changes; ordinary progress can stale evidence without invalidating routing approval. Do not mark execution `executed` until it runs, or imply model approval is user acceptance of the implementation.

## MR-08

1. Preserve the old approval and immutable `styling/v001` artifacts. The six-output v002 batch and four added pages create a changed decision identity even with Swift/medium unchanged.
2. **Card:** Phase: `ui-ux-style / generation`. Scope/artifacts: six candidates in `W/styling/v002/`, covering the expanded scope including four additional pages. Model: Swift; reuse only the generator identity actually recorded, otherwise tool-managed/unknown. Effort: reasoning medium; generator effort only if exposed; candidate count six. Reason: material expansion and materially higher generator cost. Cheaper alternative: retain the already authorized two-candidate v001 scope, which does not cover the new request. Estimate: total cost/time unknown; material cost rise is supplied by the fixture, not assumed proportional to candidate count.
3. **User message:** “Six v002 candidates with four added pages materially increase generation cost. The routing contract requires a new expenditure decision. Do you approve this expanded batch with Swift/medium?”
4. Create a new routing decision referencing the earlier record, `pending`, execution `waiting-for-approval`; establish actual settings only after the required decision and before generation.

The v001 approval remains applicable to its original scope. The unrelated prototype approval retains its own scope; neither covers v002. Any affected visual acceptance must later name the new artifacts.

## MR-09

1. Preserve the user's Deep/xhigh rejection and its conversation source. Stop this build; neither deadline nor preparation authorizes that rejected configuration or automatic execution of an alternative.
2. Inspect only scope/capability metadata needed to assess Swift/medium as the cheaper alternative; do not perform production edits.
3. **Alternative card:** Phase: `ui-ux-build / implement`. Scope/artifacts: the already authorized implementation batch and its production targets. Model: Swift. Effort: medium. Reason: lower fixture cost, with reduced reasoning capacity; scoped adequacy must be established before execution. Cheaper alternative: none further known. Estimate: totals unknown; the fixture supplies only a relative cheaper choice.
4. **User message:** “Deep/xhigh is rejected and will remain so. Swift/medium is the available cheaper alternative, with less reasoning capacity. Do you choose Swift/medium for this implementation batch?”
5. Keep the rejected decision `rejected`; add the distinct proposed alternative as `pending`, execution `waiting-for-approval`, with no invented user choice. Once the user specifically selects an adequate alternative, record that actual decision, establish effective settings and then resume.

Product authorization remains valid; execution under a particular alternative is unresolved. Do not repeat the rejected proposal or rewrite rejection as approval.

## MR-10

1. Keep the Deep/high review decision `approved`. Record execution `waiting-for-switch`; do not assess implementation or run review checks with Swift/medium.
2. **Current card:** Phase: `ui-ux-review / review`. Scope/artifacts: the existing approved review scope and `W/verification/` outputs. Model: Deep. Effort: high. Reason: already selected and approved; the current host has no switching/delegation control. Cheaper alternative: active Swift/medium, not an authorized substitute. Estimate: cost/time unknown; no usable rates/workload supplied.
3. **User message:** “Deep/high is approved, but this host cannot switch or delegate this review. The routing contract requires established effective settings before review. Please manually switch to Deep/high and confirm the effective model and effort.”
4. In `W/model-routing.md`, preserve approval/source; record the fixture's reported active Swift/medium and the inability to inspect effective settings, rather than inventing host confirmation. Record the intended executor and blocker. A shell-started CLI would be a separate process and is not evidence of switching this assistant.

Resume upon host evidence or attributed user confirmation that the actual review executor now uses supported Deep/high, resolving conflicting observations. Label user-attested evidence accurately. Configuration-file text, recommendation or the deadline cannot establish the runtime state; independent authorized preparation may continue.

## MR-11

1. Read scope metadata and present the choice before the focused audit's assessment/checks.
2. **Card:** Phase: `ui-ux-review / focused audit`. Scope/artifacts: the authorized focused review and findings/evidence in `W/verification/`. Model: Swift. Effort: medium. Reason: confirmed active configuration is adequate, with no material cost/time/quality change. Cheaper alternative: none known to be available and adequate. Estimate: cost unknown and duration unknown; pricing, token volume and observed workload timing are unavailable.
3. **User message:** “I’ll run this focused audit with confirmed Swift/medium. There is no identified material change requiring a routing decision; cost and duration estimates are unknown.”
4. Create a routing record with approval-required `no`, decision `not-required`, reason as stated, actual Swift/medium and the supplied confirmation provenance. Execution becomes `ready`; then the audit may begin. Record `executed` only after it actually runs and retain observed findings separately.

No additional approval is required solely because estimates are missing. `not-required` is the agent's impact assessment, not a fabricated user approval. Report unrun checks as unverified; even a completed passing audit does not establish user acceptance of the implementation. Record acceptance only from an actual still-applicable user decision.

## MR-12

1. Stop plan drafting: Deep/xhigh is unsupported, and the configuration-file edit has not established the requested runtime. Do not relabel xhigh as high or claim the switch succeeded.
2. Preserve the user's actual Deep/xhigh approval and source. Its execution is `blocked` for unsupported effort; execution history records host-reported Deep/medium, supported efforts medium/high, and the conflicting config text as insufficient runtime evidence.
3. **Replacement card:** Phase: `ui-ux-plan / drafting`. Scope/artifacts: the same scoped production plan, `W/plan.md`. Model: Deep. Effort: high, supported by the supplied host report. Reason: supported planning default; less effort than the approved but unavailable xhigh, and higher than actual medium. Cheaper alternative: Deep/medium only if pricing establishes it as cheaper and the user explicitly chooses lower planning effort; no pricing is supplied. Estimate: total cost/time unknown.
4. **User message:** “Deep/xhigh is unavailable; effective settings still report medium. The routing contract prevents an unsupported or unestablished selection from running. Do you choose supported Deep/high for this plan?”
5. Record the distinct alternative as `pending` with the actual impact assessment, preserving the earlier approval history. After the specific supported choice, establish effective Deep/high via authorized control or manual confirmation; only then mark execution `ready` and draft.
