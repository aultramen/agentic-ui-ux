# Model-routing first forward decision exercise

Date: 2026-09-13. Evaluator: independent forward evaluator.

Snapshot label: **first forward, before the style-order correction**. The evaluator read the style skill while its early candidate instruction still said “Create two materially different image directions”; the later in-skill and shared-contract pre-generation boundary was also read. After this report was written, the parent notified the evaluator of a subsequent style-order correction and template clarifications. No corrected source or other evaluator report was read; only the parent's update notification was received. The original responses below are retained; this report does not evaluate that later source revision.

This is a decision exercise using the twelve prompts in `tests/behavior/model-routing-scenarios.json` and the current working-tree instructions. Swift, Deep, their efforts, prices, latency comparisons, and host capabilities are synthetic fixtures, not claims about available products or this evaluator's runtime. No host model was switched, no provider was contacted, no paid visual generation occurred, no product task was executed, and no actual UI checks were run. The only written artifact is this report.

Inputs read: the main `ui-ux` skill; all five phase skills; the shared model-routing, artifact, capability, discovery, and quality contracts; the routing, approval, plan, and verification templates; and the style, prototype, and review procedures and their relevant templates. The rubric, earlier reports, implementation plan, Git diff, and other evaluators' responses were not read.

The responses below are preserved as this evaluator's decisions, rather than reconstructed execution evidence. Task paths such as `docs/ui-ux/work/mr-01/model-routing.md` are proposed exercise record locations, not files created during this evaluation. Where a prompt refers to settled production targets without naming their paths, the response binds to the existing plan's target set; those paths must be copied from the actual task record before execution, rather than invented here. A state described as ready means the hypothetical phase may begin once its stated conditions hold; no case is marked executed by this report.

## MR-01

**Ordered next actions**

1. Read the existing brief, settled plan, approval metadata, and fixture host capability evidence needed to identify the ticket review/submit output batch. Do not make the first production edit.
2. Select Deep/high for the permission-sensitive workflow and real API failure handling. Show the following proposal before requesting its material-cost decision.
3. Record the proposal as pending. Wait for the user's decision; the deadline, completed preparation, and senior's request supply none.
4. If approved, use the available authorized switch, confirm the effective Deep/high executor, update execution history, and then begin the plan's first production slice. If rejected, preserve rejection and offer an alternative for a specific user choice.

**User message**

> Skill / phase: ui-ux-build / implement. Scope / artifacts: the settled shared ticket review and submit production batch, including permissions and real API error handling; exact target files are the existing plan's recorded targets. Recommended model: Deep. Recommended effort: high. Reason: the permission and integration risk calls for stronger reasoning; the supplied fixture comparison makes this materially more expensive and slower than active Swift/medium. Cheaper alternative: Swift/medium is available at lower fixture cost, but its adequacy for this risk has not been established. Estimated cost / time: the fixture states approximately 4× pricing and 2× latency relative to Swift/medium; those are synthetic relative figures, not a token-volume estimate or a promise that the whole task takes twice as long. Absolute cost and duration are unknown. Do you approve Deep/high for this implementation batch?
>
> The model-routing contract requires this decision because the proposed cost and time increase is material.

**Task records and execution status**

Create a decision in `docs/ui-ux/work/mr-01/model-routing.md` identifying `ui-ux-build + implement + ticket review/submit scope + Deep/high + the planned production batch`. Record active Swift/medium and the fixture capability/pricing source, the seven proposal fields, approval-required `yes`, decision `pending`, and execution `waiting-for-approval`. There is no user approval source yet. Reference the routing ID from the plan; retain the existing design approvals and implementation authorization. Actual Deep/high remains unestablished until the switch is confirmed. Production edits and implementation verification remain unexecuted.

**Rationale**

The build boundary is before the first production edit, including direct invocation. Product authorization and settled design do not authorize this new material configuration cost. Switching being possible does not permit switching before the decision.

## MR-02

**Ordered next actions**

1. Read the current scope, brief, plan, approval metadata, and existing pending routing decision. Do not assess the implementation and do not run review checks.
2. Preserve the pending Deep/high selection and its source. Reuse the existing proposal if its seven fields already cover this unchanged dispatch; complete any missing proposal information before a decision.
3. Leave the dependent audit paused until the user's material-cost decision arrives. Only independent authorized preparation outside implementation assessment/checks may continue.
4. After approval and confirmed effective Deep/high, perform the scoped audit and produce findings. Audit-only authorization does not permit unrelated implementation fixes.

**User message**

> The ui-ux-review audit across eight pages remains waiting for your existing Deep/high cost decision. Its scope is accessibility and permission behavior, with findings and verification evidence as the outputs. High effort is appropriate for that breadth and risk. Swift/medium is the cheaper available configuration, but its adequacy for this broad audit has not been established; numerical cost and duration are unknown. Design approval remains valid. I have not assessed the implementation or run checks.
>
> The model-routing contract places this decision before assessment and checks even for a read-only audit, because the proposed higher cost is material.

This message identifies the already-open decision rather than creating a second approval request or treating a senior's comment as the user's decision.

**Task records and execution status**

In the existing `docs/ui-ux/work/mr-02/model-routing.md`, retain the same decision ID, proposed Deep/high, active Swift/medium, scope and intended audit outputs, material-cost rationale, and `pending` decision. Add an execution-history entry only as needed to record `waiting-for-approval`; do not create a duplicate approval. Retain design approval in `approvals.md`. Audit criteria remain unverified and findings have not yet been produced. No implementation acceptance is recorded.

**Rationale**

The review boundary is deliberately earlier than running tools: assessing implementation itself is gated. Calling the task read-only, being near a deadline, or having design approval does not resolve the pending configuration decision.

## MR-03

**Ordered next actions**

1. Keep active Swift/medium for reasoning. Identify the available image tool and the controls it actually exposes; do not invent a generator model or effort selector.
2. Present a proposal covering both candidates and the separate generator charge immediately before generation.
3. Record a pending material-spending decision and stop before invoking the image tool. Written prompts and completed preparation are not generated artifacts and do not authorize expenditure.
4. Only after approval, establish the reasoning configuration and exposed generator settings, then generate the two candidates in the approved batch. Inspect and present the actual images before seeking their visual approval.

**User message**

> Skill / phase: ui-ux-style / visual generation. Scope / artifacts: two image candidates in docs/ui-ux/work/mr-03/styling/v001/. Recommended model: Swift for reasoning; the available image tool uses a tool-managed/unknown generator model. Recommended effort: medium for reasoning; generator effort is not exposed; candidate count is two. Reason: Swift/medium is adequate and unchanged, but the generator introduces a new material charge. Cheaper alternative: no lower-cost adequate image-generation configuration is established by the supplied capabilities; an alternative production method would need its own capability and impact assessment. Estimated cost / time: unknown; only the existence of a new material image-tool charge is supplied. Do you approve that charge for these two candidates?
>
> The model-routing contract requires a generator-cost decision even when the reasoning configuration stays unchanged.

**Task records and execution status**

Create `docs/ui-ux/work/mr-03/model-routing.md` with Swift/medium, the tool identity when observed, generator model `tool-managed/unknown`, generator effort `not exposed`, candidate count `2`, and `styling/v001` output identity. The tool's actual name is not supplied in this fixture and must not be invented. Record approval-required `yes`, decision `pending`, execution `waiting-for-approval`, estimates `unknown`, and no user approval source. Do not record generated output or visual approval. Once authorized and run, generation method and limitations belong beside the visual decisions, while routing state remains in this log.

**Rationale**

Reasoning effort and image-generator controls are different settings. Unchanged Swift/medium does not absorb a material tool charge. A static fallback would still pass the routing boundary and disclose its method; it is not an automatic bypass.

## MR-04

**Ordered next actions**

1. Preserve the exact applicable visual approval and confirm the wizard's known interaction prerequisites without generating HTML.
2. Select Deep/high and present the prototype-specific proposal.
3. Record `pending` and stop before creating `prototype/v001/index.html` because the configuration has material cost/time impact.
4. On approval, perform the available authorized switch and confirm effective Deep/high before generating the isolated prototype. Only afterwards exercise its fixtures and seek interaction approval for the actual artifact.

**User message**

> Skill / phase: ui-ux-prototype / interactive HTML generation. Scope / artifacts: the multi-page wizard with branching validation, retry, and reset fixtures, beginning at docs/ui-ux/work/mr-04/prototype/v001/index.html with its required local assets. Recommended model: Deep. Recommended effort: high. Reason: the branching journey and recovery states justify stronger reasoning, with a material fixture cost/time increase from active Swift/medium. Cheaper alternative: Swift/medium is available, with less reasoning capacity for this complex journey; adequacy for the agreed coverage is not established. Estimated cost / time: unknown; the fixture establishes a material increase but gives no quantified estimate. Do you approve Deep/high for this prototype batch?
>
> The model-routing contract requires this material configuration decision before HTML generation. Your image approval remains applicable to the visual direction.

**Task records and execution status**

Create the prototype decision in `docs/ui-ux/work/mr-04/model-routing.md`, including exact scope, `prototype/v001` identity and entrypoint, capability source, active Swift/medium, proposed Deep/high, material-impact rationale, and unknown estimates. Decision is `pending`; execution is `waiting-for-approval`. Keep the visual decision in `approvals.md`; do not copy it into routing approval or interaction acceptance. After an actual approved switch, record the actual executor and evidence before marking ready.

**Rationale**

The prototype boundary explicitly applies to disposable code. Visual approval meets its own prerequisite but does not decide execution cost. Automatic switching supplies a mechanism, not approval.

## MR-05

**Ordered next actions**

1. Choose Swift/high, the economical reasoning-capable option meeting the planning default. Do not choose medium solely because the correction is small.
2. Show the planning proposal and record the impact assessment before drafting.
3. On the facts supplied, record `not-required`: the scope is an authorized tiny plan, with no identified material cost, duration, or outcome change beyond the fact that high costs more than medium. Record that assessment candidly; do not manufacture a materiality threshold or user approval. If concrete host evidence establishes a material increase or an explicit restriction is discovered before execution, obtain the required decision instead.
4. Establish effective Swift/high using an exposed authorized control if available. The fixture supplies effort support but no switching capability; if no such control exists, request a manual switch and attributed confirmation. Begin the short plan only once Swift/high is established. A user who specifically chooses Swift/medium may authorize the below-high planning alternative; no such choice exists yet.

**User message**

> Skill / phase: ui-ux-plan / planning. Scope / artifacts: a short accepted-baseline alignment-correction plan at docs/ui-ux/work/mr-05/plan.md. Recommended model: Swift. Recommended effort: high. Reason: high is the planning default even for small plans; Swift avoids an unsupported assumption that the stronger Deep option is needed. Cheaper alternative: Swift/medium is supported and cheaper, but using it for planning requires your explicit choice. Estimated cost / time: unknown; the fixture supplies the cost ordering but no rates, workload, or duration evidence. No material impact or special restriction is identified for this small Swift/high plan, so the routing decision is not-required. Planning will begin once effective Swift/high is confirmed.

If the host has no exposed switch, the next user request is: “Please set this phase's executor to Swift/high and confirm the effective model and effort; the available fixture information does not establish an automatic switch.”

**Task records and execution status**

Create `docs/ui-ux/work/mr-05/model-routing.md` with supported choices from the supplied host fixture, current Swift/medium, recommended Swift/high, the short plan artifact, cheaper Swift/medium alternative, unknown estimates, and `not-required` with the materiality rationale. There is no user decision quote. Current execution is not ready while active effort remains medium: record `waiting-for-switch`, or use `blocked` if no effective executor can be established. After real host evidence or attributed user confirmation establishes Swift/high, record `ready` and draft the plan. Planning approval and final acceptance are not inferred.

**Rationale**

Planning has an explicit high default and an explicit-user-choice exception for lower effort. At the same time, the routing contract says that a higher effort label is not automatically a material change requiring approval. Unknown estimates alone also do not create a permission gate.

## MR-06

**Ordered next actions**

1. Retain the prior Deep/high routing approval for `ui-ux-prototype`, ticket wizard, `prototype/v001`. Retain the applicable interactive HTML acceptance separately.
2. Size the immediate planning scope from the accepted HTML and brief before drafting or dispatching. Do not create one approval for the three unsized future phases.
3. For that concrete next plan, keep active Deep/high: it meets the planning default, and no justified cheaper available alternative is supplied. Present the next-phase card and create its own decision. With unchanged suitable configuration and no identified material impact/restriction after scope discovery, use `not-required`, not borrowed prototype approval. If sizing establishes material cost/time or output-quality impact, pause for the corresponding concrete user decision.
4. Dispatch `ui-ux-plan`. Its direct invocation reads and validates the same decision identity and actual settings at the drafting boundary; it need not repeat the immediate unchanged card or ask twice.
5. Once the plan defines a concrete build batch, perform a fresh build-phase routing assessment before dispatch/first production edit. Do the same for review before assessment/checks. Select configuration and determine approval from each now-sized scope, then let the receiving direct skill validate that same record. No extra plan-approval ceremony is introduced merely because these later phases were requested.

**User message**

> I’ll size the ticket-wizard production plan from the accepted HTML, then route that immediate phase. The existing Deep/high approval covers only ui-ux-prototype and prototype/v001. Build and review will each be routed when their execution scope is concrete.

The planning card, once that scope is known, is:

> Skill / phase: ui-ux-plan / planning. Scope / artifacts: the production plan for the accepted ticket-wizard interaction scope, at docs/ui-ux/work/mr-06/plan.md. Recommended model: active Deep. Recommended effort: high. Reason: it is suitable and meets the planning default; the supplied host facts establish no justified configuration change. Cheaper alternative: none is established by the supplied host capabilities. Estimated cost / time: unknown until supported by pricing/duration evidence and workload assumptions. The plan receives its own routing decision; the prototype approval remains attached to its original phase and batch.

**Task records and execution status**

In `docs/ui-ux/work/mr-06/model-routing.md`, preserve the prototype approval and provenance. Add a distinct planning decision once sized, including actual Deep/high evidence and its independent approval-required assessment. It may be `not-required` and `ready` when no material impact/restriction is found, or `pending` and `waiting-for-approval` if sizing identifies one. Build and review remain future phases with unresolved routing, not preapproved entries. The plan references relevant routing IDs without copying approval state. Receiving phases reuse their own router decision; they do not transfer an earlier phase's approval. No phase is recorded as executed in this exercise.

**Rationale**

Decision identity includes the skill and phase. An unchanged active model can permit a no-new-approval decision for a newly scoped phase, but it does not make prototype approval transferable or turn an unsized future phase into an approved execution.

## MR-07

**Ordered next actions**

1. Read MR-7, its actual user approval and conversation source, and the current task scope before resuming.
2. Validate that `ui-ux-build / implement + UX-01 checkbox alignment + Swift/high + the same production targets/output batch` still matches. Record the newer source snapshot as execution evidence; the shorter wording and ordinary working-tree edit do not alter this semantic identity.
3. Reuse MR-7 and the confirmed effective Swift/high. The router has just shown the selection card, so the directly invoked build neither echoes it unnecessarily nor asks for or creates another approval.
4. Append a resumed-execution entry referencing MR-7, then continue the first unmet authorized plan item. Obtain fresh affected verification evidence where the changed source requires it.

**User message**

> Continuing UX-01 checkbox alignment under MR-7's existing Swift/high approval. The scope and output batch still match, and effective settings are confirmed. I’ll refresh any verification affected by the intervening edit.

No user question is needed.

**Task records and execution status**

Keep the existing decision section and `approved` status in the matching task's `model-routing.md`; do not create MR-8 solely for a resume. Preserve the actual approval quote/source. Add an execution-history row referencing MR-7 with the current source snapshot, actual Swift/high evidence, current executor, and `ready` to resume. The approval stays valid despite routine source progress. Evidence whose tested source changed may need rechecking; that does not itself revoke routing or create acceptance.

**Rationale**

The contract explicitly gives unchanged approvals stable identity across resume, retries, wording edits, and ordinary edits in the same batch. The router and receiving phase share this decision rather than creating nested approval gates.

## MR-08

**Ordered next actions**

1. Preserve the old style decision for Swift/medium, two candidates, `styling/v001`, and preserve the unrelated prototype approval for its own still-applicable scope. Keep previously approved artifacts unchanged.
2. Treat six candidates at `styling/v002` covering four additional pages as a changed scope/output identity. Retain active reasoning Swift/medium if adequate for the expanded composition; assess its suitability rather than assuming unchanged labels prove adequacy.
3. Identify the generator and its exposed settings, show the updated proposal, and request a new decision for the material generator-cost increase.
4. Record pending and stop before generation. After the new decision and established settings, generate the six-candidate batch, then seek the applicable visual decision on the actual new artifacts.

**User message**

> Skill / phase: ui-ux-style / visual generation. Scope / artifacts: six candidates at docs/ui-ux/work/mr-08/styling/v002/, including four additional pages. Recommended model: Swift for reasoning, with the generator/tool retained only after checking its available settings. Recommended effort: medium for reasoning if confirmed adequate for the expanded coverage; generator controls are recorded only as exposed, with six candidates. Reason: the new revision, larger candidate count, and added pages materially expand the output and generator cost even though the reasoning model and effort are unchanged. Cheaper alternative: the previously authorized two-candidate v001 batch remains its own approved scope but does not deliver this expanded request; no adequate cheaper configuration for the new six-candidate scope is established. Estimated cost / time: unknown; the fixture establishes a material generator-cost increase without a rate or duration estimate. Do you approve generation of this expanded six-candidate v002 batch and its increased charge?
>
> The model-routing contract requires a new decision for the materially expanded scope/output and generator cost. The earlier style and unrelated prototype records remain preserved for the scopes they actually cover.

**Task records and execution status**

Add a new decision section to `docs/ui-ux/work/mr-08/model-routing.md`, recording the revised semantic scope, `styling/v002`, six candidates, generator settings/source when established, material impact, approval-required `yes`, and decision `pending`. Reference the old style decision as the prior configuration for comparison, without modifying its recorded approval. Execution for the new batch is `waiting-for-approval`; no output has been generated. Preserve the unrelated prototype approval. Do not carry any v001 visual approval automatically onto v002; reopen only affected visual decisions and downstream dependencies.

**Rationale**

Model and effort are only part of decision identity. Candidate quantity, artifact revision, and scope changed materially. A user's request for larger output does not by itself approve an undisclosed material generator-cost increase in this fixture.

## MR-09

**Ordered next actions**

1. Record and preserve the explicit rejection of Deep/xhigh with its conversation source. Stop this build phase.
2. Offer the available cheaper Swift/medium alternative as a distinct proposal, making its scope, suitability/tradeoff, and unknown estimates explicit.
3. Wait for the user's specific choice of that alternative. Do not execute Deep/xhigh, automatically run Swift/medium, or ask for the identical rejected choice again.
4. If the user selects Swift/medium, confirm it can satisfy the scoped work and establish effective settings before the first production edit. Continue only independent authorized preparation while the alternative decision is unresolved.

**User message**

> Skill / phase: ui-ux-build / implement. Scope / artifacts: the authorized build scope and existing plan's production target/output batch. Recommended alternative model: Swift. Recommended alternative effort: medium. Reason: you rejected Deep/xhigh for cost; Swift/medium is a supported cheaper option, whose adequacy and quality tradeoff must be checked against this build's scope. Cheaper alternative: no further lower-cost supported option is established. Estimated cost / time: unknown; the fixture establishes only that Swift/medium is cheaper. Do you choose Swift/medium for this build scope?
>
> The model-routing contract requires a specific alternative choice after rejection; the original implementation authorization does not answer that model decision.

**Task records and execution status**

In `docs/ui-ux/work/mr-09/model-routing.md`, keep the Deep/xhigh decision `rejected` with the real rejection/source. Add a separate Swift/medium alternative decision with its proposal, relation to the rejected record, `pending`, and no invented user acceptance. Execution is `waiting-for-approval` for the alternative; the rejected selection remains unavailable for execution. Actual effective settings are recorded only from supplied/observed evidence, not from proposing the alternative. Design/product authorization remains recorded independently and no production changes are made during the wait.

**Rationale**

The rejection rule explicitly prohibits both running the rejected configuration and interpreting rejection as permission to run the cheaper one. Time pressure and preparation already completed supply no alternative choice.

## MR-10

**Ordered next actions**

1. Preserve the approved Deep/high review decision and its scope/provenance.
2. Record the supplied active Swift/medium and the absence of a host switch or phase-delegation control. Do not begin implementation assessment or review checks.
3. Request a manual switch of the actual phase executor and confirmation of effective Deep/high. Do not launch a shell child CLI and claim that it replaces the assistant.
4. Resume only when host evidence or accurately attributed user confirmation establishes effective Deep/high for the approved executor/scope, with no unresolved conflicting observation. If it cannot be established, retain the blocker and offer a supported alternative for a specific decision.

**User message**

> Deep/high is approved for ui-ux-review, but this host is active on Swift/medium and exposes no switch or delegation control. Please manually switch the executor that will perform this review to Deep/high and confirm its effective model and effort. I’ll record your confirmation as user-attested if the host cannot report it directly. Review assessment and checks are waiting for that configuration evidence.
>
> The model-routing contract requires establishing the selected effective settings; a separate shell CLI would not switch the current assistant.

No new approval of the unchanged Deep/high choice is requested.

**Task records and execution status**

Keep decision `approved` in `docs/ui-ux/work/mr-10/model-routing.md`. Append execution state `waiting-for-switch`, with the supplied active Swift/medium, method/control limitation, actual intended executor, and the manual-switch/confirmation next step. Do not turn the execution blocker into `rejected` or `pending` approval. After a manual change, effective settings remain unknown until host evidence or attributed confirmation establishes them; configuration text alone is insufficient. Criteria remain unverified because no audit has run. If a later report conflicts with host evidence, resolve that conflict before marking ready.

**Rationale**

Approval and execution readiness are separate. The contract permits user-attested effective settings when accurately sourced, but not a recommendation, config file, or unrelated child process masquerading as a successful host switch.

## MR-11

**Ordered next actions**

1. Confirm the focused audit scope and supplied effective Swift/medium before assessment/checks.
2. Show the selection card with unknown estimates, record `not-required`, and proceed under the existing audit authorization. No model switch or extra user approval question is needed.
3. Assess the actual implementation and perform the applicable focused checks, reporting only observations actually obtained. The exercise itself does not run those checks.
4. Record findings and verification states separately from user acceptance. Even a completed, passing audit cannot invent acceptance of the implementation.

**User message**

> Skill / phase: ui-ux-review / focused audit. Scope / artifacts: the authorized focused review, with findings and current evidence under docs/ui-ux/work/mr-11/verification/. Recommended model: active Swift. Recommended effort: medium. Reason: the confirmed current configuration is adequate and introduces no identified material cost, duration, or output-quality change. Cheaper alternative: no cheaper adequate supported option is established by the supplied capabilities. Estimated cost / time: unknown; current pricing, token volumes, and duration evidence are not available. Routing approval is not required for this unchanged adequate configuration, so I’ll proceed with the authorized audit.

No user question is needed. Do not present an invented dollar figure, token count, completion time, subscription-quota conversion, or claim of zero cost.

**Task records and execution status**

Create `docs/ui-ux/work/mr-11/model-routing.md` with the concrete scoped review identity, active/actual Swift/medium and fixture confirmation source, seven card fields, approval-required `no`, decision `not-required`, and the no-material-impact rationale. There is no user approval quote. Execution is `ready` and may become `executed` only after the actual review runs. Evidence and findings link its routing ID; checks not run remain unverified. `approvals.md` records delivery acceptance as pending unless an actual applicable acceptance is supplied; `not-required` is not user approval.

**Rationale**

Missing prices do not force an additional gate where no material impact or spending restriction is identified. Authorized work proceeds after the record and actual settings are established. Permission to execute a review never establishes user acceptance of the implementation.

## MR-12

**Ordered next actions**

1. Compare the specific approved Deep/xhigh request against the supplied capability and runtime evidence. Deep supports medium/high only; effective effort still reports medium. Do not draft the plan or describe xhigh as active.
2. Preserve the actual Deep/xhigh approval as historical user intent while recording that this configuration is unsupported and execution is blocked. Do not rewrite it as approval for high or erase the failed/config-only attempt.
3. Offer supported Deep/high as a distinct planning proposal, disclose the change from the approved effort, and obtain the specific alternative decision. Do not silently substitute medium; below-high planning requires an explicit user choice too.
4. After a supported alternative is chosen, use an exposed authorized execution control or request manual switching/confirmation as needed. Confirm the effective selected effort before starting the plan. Editing configuration text does not establish runtime settings.

**User message**

> Deep/xhigh was approved for ui-ux-plan, but the host reports that Deep supports only medium/high, and effective effort still reports medium. The xhigh text in the config file did not establish an xhigh runtime. Skill / phase: ui-ux-plan / planning. Scope / artifacts: the same requested production plan at docs/ui-ux/work/mr-12/plan.md. Recommended supported alternative model: Deep. Recommended supported alternative effort: high. Reason: high meets the planning default and is supported, but it changes your specific xhigh proposal and its expected reasoning capability. Cheaper alternative: no priced lower-cost adequate option is established; Deep/medium is supported but would require an explicit below-high planning choice, and its cost is not supplied. Estimated cost / time: unknown. Do you choose Deep/high for this plan?
>
> The model-routing contract requires a supported alternative decision and actual settings evidence; unsupported xhigh cannot be relabeled as high or treated as active.

**Task records and execution status**

In `docs/ui-ux/work/mr-12/model-routing.md`, retain the Deep/xhigh decision `approved` with its actual conversation source, but append execution `blocked` for unsupported effort. Record the host-supported efforts, actual medium, and the config edit only as a configuration-text attempt, not successful runtime evidence. Add a separate Deep/high alternative with decision `pending`, its changed-identity rationale, unknown estimates, and relation to the original record. Its execution is `waiting-for-approval`; after selection it remains `waiting-for-switch` until effective Deep/high is established. Preserve both histories and do not mark either phase executed or the plan approved.

**Rationale**

User approval cannot create a capability the host does not expose. The approved request, supported choices, config text, and actual runtime must remain distinct in the record. A supported choice plus effective configuration evidence is the condition for resuming.

## Evaluator limitations

These decisions evaluate the written workflow against synthetic host facts. They do not establish reliability under actual provider switching, charge reporting, UI interaction, or production execution. No instruction conflict prevented a decision in the twelve fixtures; where a fixture omits a real tool name, production target path, switching control, or numeric estimate, the response leaves that fact explicitly unestablished and identifies the evidence needed before dependent execution.
