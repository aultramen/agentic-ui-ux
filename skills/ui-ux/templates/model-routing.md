# Model routing: <task-id>

Follow the installed skill's shared routing contract. Store this record in the target repository's task folder and keep only relevant fields. This log grants no design, implementation or delivery acceptance.

## <MR-001>: <skill> / <phase>

- Date / conversation reference:
- Scope / acceptance IDs:
- Intended artifacts: paths, revisions/output batch, count:
- Active or still-applicable approved configuration / source:
- Recommended concrete model / supported effort / capability source:
- Visual generator/tool and exposed settings, or not applicable/tool-managed:
- Rationale and material cost/time/capability impact:
- Cheaper supported alternative / tradeoff, or none known:
- Estimated cost / duration, source and workload assumptions, or unknown:
- Approval required: yes/no, with reason:
- Decision: pending / approved / rejected / not-required:
- Actual user decision and conversation source, if any:
- Reused or superseded decision ID, if any:

## Execution history

| Date / reference | Decision ID | Executor / method | Actual model / effort; generator if relevant | Host evidence or attributed user confirmation | Execution state / blocker / next step |
| --- | --- | --- | --- | --- | --- |

Execution may be ready, waiting-for-approval, waiting-for-switch, blocked, or executed. Record executed only after the phase runs; it does not mean its output passed verification. Unknown effective settings remain unknown until established. Preserve previous entries when a decision changes or an execution resumes.

Use one decision section for each distinct selection; reference it for unchanged executions. `not-required` records the agent's impact assessment, not the user's approval. Never manufacture a quote, mark a pending choice approved, transfer an approval to another phase, or convert rejection into acceptance of a cheaper alternative. Design and delivery decisions stay in `approvals.md`.
