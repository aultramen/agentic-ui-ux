interface:
  display_name: "UI/UX Workflow"
  short_description: "Route web UI work from intent to verified delivery"
  default_prompt: "Use $ui-ux to inspect this web UI task and choose the appropriate workflow."
policy:
  allow_implicit_invocation: true
