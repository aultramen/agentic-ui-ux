For requests to build, improve, align, or review web/webview UI or UX, load the installed `ui-ux` skill. This includes requests such as "rapikan tampilan", "samakan flow", and "buat dashboard". Keep backend-only work and authoring this framework outside that trigger.

Inspect the project's implementation and references before asking questions. Select a workflow proportional to the change. Reuse applicable approved designs, preserve business rules, and verify the agreed acceptance criteria. Never report failed, blocked, or unverified criteria as complete. Preserve the user's scope and existing authorization; do not request an approval again when it still applies.

If automatic selection misses, explicitly invoke `ui-ux` using the current platform's skill mechanism or read its installed `SKILL.md`. Load only the references needed for the current phase. Project artifacts belong under the target repository's `docs/ui-ux/`.

Before phase execution, follow the selected skill's shared model/effort routing contract: show the choice, resolve material-impact approval, reuse unchanged decisions and establish actual settings. Record routing in the task's `model-routing.md`; permission to execute is separate from approval of designs or results. Direct phase invocation still checks its own boundary.
